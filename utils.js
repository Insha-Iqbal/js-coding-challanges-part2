export function capitalize(str) {
    return str.toUpperCase();
}

export default function add(a, b) {
    return a + b;
}

export const PI = 3.1417;

export function subtract(a, b) {
    return a - b;
}